import { MouseResponseDirective } from './mouse-response.directive';

describe('MouseResponseDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseResponseDirective();
    expect(directive).toBeTruthy();
  });
});
